<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AGAPLinkProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AGAPLinkProject" parent="Base.Theme.AGAPLinkProject" />

    <!-- CIRCULAR SHAPE FOR THE BOTTOM NAVIGATION SELECTED-STATE INDICATOR -->
    <style name="ShapeAppearance.AgapLink.NavIndicator" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <!-- SCREEN 4: SINGLE-LETTER WEEKDAY HEADER LABEL (S M T W T F S) ABOVE THE CALENDAR GRID -->
    <style name="CalendarWeekdayLabel">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/slate_400</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>